INDEX.HTML
-Overwrite multiple aspects (font, color, alignment, etc.) of several existing bootstrap styles – Do not edit the bootstrap css files. Link your stylesheet after the links to the bootstrap stylesheet so your styles get priority.
-Custom background image

ABOUTME.HTML
-Again, multiple style customizations that override the bootstrap styles (font, color, images, alignment, etc.)
-At least one embedded video that plays directly on your page (or on homepage)

